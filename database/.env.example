# Oracle connection settings
PORT=4000
ORACLE_USER= av #system
ORACLE_PASSWORD=agri #your_password
ORACLE_CONNECT_STRING=localhost:1521/XEPDB1

# Optional table/column overrides if your schema uses different names
ORACLE_TABLE=CROP_ANALYSES
ORACLE_COL_CROP=CROP_NAME
ORACLE_COL_STATUS=STATUS
ORACLE_COL_CREATED_AT=CREATED_AT
ORACLE_COL_SCORE=HEALTH_SCORE

# Optional custom SQL (uncomment to use your own queries)
# ORACLE_SUMMARY_SQL=SELECT COUNT(*) AS TOTAL_ANALYSES FROM CROP_ANALYSES
# ORACLE_RECENT_SQL=SELECT CROP_NAME, STATUS, CREATED_AT FROM CROP_ANALYSES ORDER BY CREATED_AT DESC
